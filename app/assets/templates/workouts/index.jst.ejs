<% workouts.forEach(function (workout) { %>
	<div class="workout" data-id="<%= workout.id %>">
		<%= workout.get('logged_date') %>
		<% debugger %>
		<% workout.activities.forEach(function (activity) { %>
		<div class="activity" data-id="<%= activity.id %>">
			
		</div>
		<% }) %>
	</div>
<% }); %>